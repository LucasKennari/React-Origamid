import React from 'react'

const ImagemProdutoAmostra = ({ src, alt }) => {
          return (
                    <img style={{ width: '400px' }} src={src} alt={alt} />
          )
}

export default ImagemProdutoAmostra
